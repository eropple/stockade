{
  "name": "@stockade/example",
  "version": "0.1.0",
  "description": "An inline example of the Stockade framework.",
  "private": true,
  "keywords": [
    "api",
    "web",
    "framework",
    "http",
    "openapi3",
    "swagger",
    "openapi"
  ],
  "author": "Ed Ropple <ed@edropple.com>",
  "homepage": "https://github.com/",
  "license": "MIT",
  "main": "dist",
  "scripts": {
    "lint": "tslint --project .",
    "clean": "find . -name \\*.js -o -name \\*.js.map -o -name \\*.d.ts | grep -v node_modules | grep -v 'jest.config.js' | xargs -- rm -- || true",
    "build": "tsc",
    "watch": "yarn run -s build --watch",
    "start": "yarn exec -s -- node ./dist/src/index.js"
  },
  "dependencies": {
    "@stockade/core": "0.1.0",
    "@stockade/http": "0.1.0",
    "@stockade/inject": "0.1.0",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
  }
}
